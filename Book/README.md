# Opening Books

Place optional Polyglot opening books in this directory.

Common filenames searched by the engine include:

```text
Human.bin
Perfect2023.bin
komodo.bin
book.bin
```

Opening-book files can be large or have separate licenses, so they are not tracked in this repository.

